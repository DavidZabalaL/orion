{
  "buildCommand": "(npx prisma migrate deploy || (sleep 15 && npx prisma migrate deploy)) && next build",
  "crons": [
    { "path": "/api/cron/reportes-programados", "schedule": "0 * * * *" }
  ]
}
